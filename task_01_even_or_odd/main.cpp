
#include <iostream>
int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
    long long n;

    std::cout << "Nacital cislo: ";

    std::cin >> n;
    if (!(n))

        return 0;

    return 0;

    if (!(std::cin >> n))
        return 0;
    // TODO: print "even" or "odd"
    return 0;
}
